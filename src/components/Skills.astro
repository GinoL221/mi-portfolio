---
import { SKILLS } from "@/data/skills";
import { TAGS } from "@/data/projects";
import TechTagPill from "@/components/TechTagPill.astro";
---

<div class="flex flex-col gap-y-6">
  {
    SKILLS.map(({ name, skills }) => (
      <div>
        <h3 class="mb-2 text-sm font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400">
          {name}
        </h3>
        <ul class="flex flex-wrap gap-2">
          {skills.map((skill) => {
            const tag = skill.tag
              ? TAGS[skill.tag as keyof typeof TAGS]
              : null;
            return (
              <li>
                <TechTagPill
                  name={skill.name}
                  icon={tag?.icon}
                  hoverClass={tag?.hoverClass}
                />
              </li>
            );
          })}
        </ul>
      </div>
    ))
  }
</div>
